# For more information, see: https://github.com/commercialhaskell/stack/blob/release/doc/yaml_configuration.md
# vim: nospell

resolver: lts-2.22

packages:
- '.'

- location:
    git:    https://github.com/AccelerateHS/accelerate.git
    commit: 3e0ea9b2138bc4575b0a570b6e0f6f8dfad47695
  extra-dep: true

- location:
    git:    https://github.com/AccelerateHS/accelerate-cuda.git
    commit: 4192d4f573326ce49e7145570c5c54716f992f15
  extra-dep: true

- location:
    git:    https://github.com/AccelerateHS/accelerate-llvm.git
    commit: a48fbc77908df54997855c9035b338d28c91d1d3
  extra-dep: true
  subdirs:
    - 'accelerate-llvm'
    - 'accelerate-llvm-native'
    - 'accelerate-llvm-ptx'

- location:
    git:    https://github.com/tmcdonell/lens-accelerate.git
    commit: c119c9c32d4fbf0d53b8eb906de03fa22ede2e87
  extra-dep: true

- location:
    git:    https://github.com/tmcdonell/linear-accelerate.git
    commit: e2eda8c657d207bfc888780b505ff5fdfdf20bb5
  extra-dep: true
  extra-dep: true

extra-deps:
- 'atomic-primops-0.8.0.2'
- 'base-orphans-0.4.4'
- 'chaselev-deque-0.5.0.5'
- 'cuda-0.7.5.0'
- 'libffi-0.1'
- 'llvm-general-3.4.6.0'
- 'llvm-general-pure-3.4.5.4'
- 'mtl-2.2.1'
- 'nvvm-0.7.5.0'
- 'SafeSemaphore-0.10.1'
- 'transformers-0.4.3.0'
- 'transformers-compat-0.4.0.4'
- 'unique-0'

# Override default flag values for local packages and extra-deps
flags:
  accelerate:
    unsafe-checks: false
    bounds-checks: true
    debug: true
    internal-checks: false
  accelerate-llvm:
    debug: true
    chase-lev: true
  accelerate-llvm-native:
    debug: true
  accelerate-llvm-ptx:
    debug: true
    nvvm: false
  lulesh-accelerate:
    cuda: true
    llvm-cpu: true
    llvm-gpu: true
  llvm-general:
    shared-llvm: true

# Extra package databases containing global packages
extra-package-dbs: []

# Control whether we use the GHC we find on the path
# system-ghc: true

# Require a specific version of stack, using version ranges
# require-stack-version: -any # Default
# require-stack-version: >= 0.1.4.0

# Override the architecture used by stack, especially useful on Windows
# arch: i386
# arch: x86_64

# Extra directories used by stack for building
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]

